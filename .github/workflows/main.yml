---
name: CI

on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - '.gitignore'
      - 'LICENSE'
      - '**.md'
  push:
    paths-ignore:
      - '.gitignore'
      - 'LICENSE'
      - '**.md'

jobs:
  build:
    name: Test Runner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      #- name: Build Docker Image
      #  run: docker build -f Dockerfile -t c-test-runner .

      #- name: Run Tests
      #  run: docker run --entrypoint "pytest" c-test-runner

      - name: Test
        run: ./test.sh
